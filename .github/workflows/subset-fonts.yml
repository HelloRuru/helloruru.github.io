name: 字體子集化

on:
  push:
    branches: [main]
    paths:
      - 'aio-view/index.html'

jobs:
  subset:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: aio-view/fonts
        run: npm install subset-font

      - name: Run subset script
        working-directory: aio-view/fonts
        run: node subset-fonts.js

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet aio-view/fonts/*.woff2; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add aio-view/fonts/*.woff2 aio-view/fonts/chars.txt
          git commit -m "chore(aio-view): 自動更新字體子集化

          Co-Authored-By: GitHub Actions <github-actions[bot]@users.noreply.github.com>"
          git push
