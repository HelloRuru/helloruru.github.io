# SEO Quest — 架構說明文件

> **專案定位**：全新 SEO 教學工具，未來擴展的基礎架構
> **部署位置**：`lab.helloruru.com/seo-quest/`
> **更新日期**：2026-02-12

---

## 📋 目錄

- [設計理念](#設計理念)
- [專案結構](#專案結構)
- [核心概念](#核心概念)
- [CSS 架構](#css-架構)
- [JavaScript 架構](#javascript-架構)
- [資料結構](#資料結構)
- [五階段循環](#五階段循環)
- [擴展指南](#擴展指南)

---

## 🎯 設計理念

### 核心原則

1. **模組化優先**
   - 每個功能獨立模組，降低耦合度
   - CSS 和 JS 完全模組化，易於維護
   - 元件可重用，避免重複代碼

2. **資料驅動**
   - 關卡內容由 JSON 定義，無需修改代碼
   - 評分規則可配置，靈活調整標準
   - 成就系統可擴展，持續增加獎勵

3. **未來友好**
   - 清晰的檔案結構，易於新增功能
   - 完整的文件說明，降低學習成本
   - 標準化的命名規範，團隊協作友好

4. **效能優先**
   - CSS 按需載入，減少首屏負擔
   - JavaScript 模組化載入，提升效能
   - localStorage 持久化，減少重複計算

---

## 📂 專案結構

```
seo-quest/
├── index.html                 # 主頁面
├── README.md                  # 專案說明
├── STRUCTURE.md              # 📍 本文件：架構說明
├── manifest.json             # PWA 設定
│
├── css/                      # 🎨 樣式（模組化）
│   ├── base/                 # 基礎層（模組化）
│   │   ├── tokens.css        # 設計變數（Palette + Semantic）
│   │   ├── fonts.css         # 字體引用
│   │   ├── reset.css         # CSS Reset
│   │   └── utilities.css     # 工具類別
│   ├── layout.css            # 版面配置
│   ├── typography.css        # 文字排版
│   ├── responsive.css        # RWD 響應式
│   ├── components/           # UI 元件（12 個檔案）
│   └── style.css             # 主樣式（@import 所有模組）
│
├── js/                       # 🧩 JavaScript（模組化）
│   ├── main.js               # 主程式入口
│   ├── config.js             # 全域配置
│   ├── core/                 # 核心系統（4 個檔案）
│   ├── modules/              # 功能模組（6 個檔案）
│   ├── phases/               # 五階段邏輯（5 個檔案）
│   ├── tools/                # 工具模式（3 個檔案）
│   └── utils/                # 工具函數（4 個檔案）
│
├── data/                     # 📊 資料檔案（JSON）
│   ├── levels/               # 關卡資料
│   │   ├── world-1/          # 世界 1（5 個檔案）
│   │   ├── world-2/          # 世界 2
│   │   └── world-3/          # 世界 3
│   ├── characters.json       # 角色資料
│   ├── achievements.json     # 成就定義
│   ├── scoring-rules.json    # 評分規則
│   └── tips.json             # 教學提示
│
├── assets/                   # 靜態資源
│   └── icons/                # 圖示（emoji/SVG）
│
└── docs/                     # 📚 文件
    ├── API.md                # API 文件
    ├── DATA-STRUCTURE.md     # 資料結構說明
    ├── SCORING-SYSTEM.md     # 評分系統說明
    └── EXTENSION-GUIDE.md    # 擴展指南
```

---

## 🎮 核心概念

### 雙模式系統

```
┌─────────────────────────────────┐
│         SEO Quest               │
├─────────────────────────────────┤
│                                 │
│  [📚 教學模式] [🛠️ 工具模式]    │
│                                 │
└─────────────────────────────────┘
```

**教學模式**（Tutorial Mode）
- RPG 風格學習體驗
- 關卡地圖 + 等級系統
- 五階段學習循環
- 成就與獎勵系統

**工具模式**（Tool Mode）
- 專業實用工具
- 快速輸入介面
- SEO 分析功能
- 無遊戲化元素

### 五階段學習循環

每個關卡包含 5 個階段，形成完整學習閉環：

```
1. Tutorial（教學） → 學習概念
         ↓
2. Demo（示範）     → 看範例（劇情）
         ↓
3. Practice（實作） → 自己做（戰鬥）
         ↓
4. Score（評分）    → 獲得反饋（結算）
         ↓
5. Levelup（升級）  → 獲得獎勵
```

---

## 🎨 CSS 架構

### 三層架構設計

```
基礎層（Foundation）
├── base/            # 基礎模組（雙層變數架構）
│   ├── tokens.css   # 設計變數（Palette + Semantic）
│   ├── fonts.css    # 字體引用
│   ├── reset.css    # CSS Reset
│   └── utilities.css# 工具類別
├── layout.css       # 版面配置系統
└── typography.css   # 文字排版系統

元件層（Components）
├── mode-switch.css  # 模式切換器
├── level-map.css    # 關卡地圖
├── dialogue.css     # 對話框
├── tutorial.css     # 教學介面
├── demo.css         # 示範介面
├── practice.css     # 實作編輯器
├── score.css        # 評分報告
├── levelup.css      # 升級介面
├── progress.css     # 進度條/經驗值
├── achievement.css  # 成就系統
├── tool-mode.css    # 工具模式
└── toast.css        # 提示訊息

響應式層（Responsive）
└── responsive.css   # RWD 斷點
```

### 設計系統變數

**顏色系統**
```css
:root {
  /* 品牌色 */
  --color-primary: #D4A5A5;      /* 玫瑰粉 */
  --color-secondary: #B8A9C9;    /* 紫羅蘭 */
  --color-accent: #98C4BA;       /* 薄荷綠 */

  /* 等級顏色 */
  --color-level-1: #98C4BA;      /* Lv.1-2 新手 */
  --color-level-3: #F4A896;      /* Lv.3-5 專員 */
  --color-level-8: #B8A9C9;      /* Lv.6-9 專家 */
  --color-level-10: #FFD700;     /* Lv.10 大師 */

  /* 評分顏色 */
  --color-score-excellent: #4CAF50;  /* 95-100 */
  --color-score-good: #8BC34A;       /* 85-94 */
  --color-score-pass: #FFC107;       /* 75-84 */
  --color-score-fail: #F44336;       /* 0-64 */
}
```

**間距系統**（4px 基準）
```css
:root {
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --spacing-3xl: 64px;
}
```

**字體系統**
```css
:root {
  --font-primary: 'GenSenRounded', 'Noto Sans TC', sans-serif;
  --font-mono: 'JetBrains Mono', 'Consolas', monospace;

  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-base: 16px;
  --font-size-lg: 18px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 32px;
  --font-size-4xl: 40px;
}
```

### CSS 命名規範

採用 **BEM（Block Element Modifier）** 命名法：

```css
/* Block */
.level-map { }

/* Element */
.level-map__item { }
.level-map__title { }

/* Modifier */
.level-map__item--locked { }
.level-map__item--completed { }
```

**狀態類別**
```css
.is-active
.is-disabled
.is-loading
.is-hidden
```

---

## 🧩 JavaScript 架構

### 模組職責劃分

#### **Core（核心系統）**
```javascript
core/
├── router.js    # 階段路由管理
├── state.js     # 全域狀態管理
├── storage.js   # localStorage 持久化
└── events.js    # 事件系統
```

**職責**：提供底層基礎功能，被其他模組依賴

#### **Modules（功能模組）**
```javascript
modules/
├── mode-manager.js   # 模式切換邏輯
├── level-system.js   # 等級計算與管理
├── progress.js       # 進度追蹤
├── achievement.js    # 成就系統
├── scoring.js        # 評分引擎
└── ai-helper.js      # AI 輔助功能
```

**職責**：實現獨立功能，可被組合使用

#### **Phases（階段邏輯）**
```javascript
phases/
├── phase-tutorial.js  # 1. 教學階段
├── phase-demo.js      # 2. 示範階段
├── phase-practice.js  # 3. 實作階段
├── phase-score.js     # 4. 評分階段
└── phase-levelup.js   # 5. 升級階段
```

**職責**：五階段的 UI 渲染與互動邏輯

#### **Tools（工具模式）**
```javascript
tools/
├── quick-mode.js  # 快速模式
├── analyzer.js    # SEO 分析器
└── checker.js     # 檢查工具
```

**職責**：工具模式的專業功能

#### **Utils（工具函數）**
```javascript
utils/
├── text-analysis.js   # 文字分析
├── keyword-check.js   # 關鍵字檢測
├── readability.js     # 可讀性計算
└── helpers.js         # 通用輔助函數
```

**職責**：純函數，無副作用，可重用

### 狀態管理

使用簡單的訂閱模式：

```javascript
// core/state.js
export const State = {
  current: {
    mode: 'tutorial',
    world: 1,
    level: '1-1',
    phase: 'tutorial',
    user: {
      level: 1,
      exp: 0,
      totalScore: 0
    }
  },

  update(key, value) {
    this.current[key] = value;
    this.notify();
  },

  subscribers: [],

  subscribe(callback) {
    this.subscribers.push(callback);
  },

  notify() {
    this.subscribers.forEach(cb => cb(this.current));
  }
};
```

---

## 📊 資料結構

### 關卡資料（Level Data）

每個關卡是一個 JSON 檔案：

```json
{
  "id": "1-1",
  "world": 1,
  "title": "關鍵字優化基礎",
  "requiredLevel": 1,
  "expReward": 150,

  "phases": {
    "tutorial": { ... },
    "demo": { ... },
    "practice": { ... },
    "scoring": { ... },
    "levelup": { ... }
  }
}
```

詳見：[DATA-STRUCTURE.md](./DATA-STRUCTURE.md)

### 評分規則（Scoring Rules）

```json
{
  "criteria": {
    "keywordSelection": {
      "weight": 30,
      "checks": [...]
    },
    "keywordUsage": {
      "weight": 30,
      "checks": [...]
    },
    "contentQuality": {
      "weight": 40,
      "checks": [...]
    }
  }
}
```

詳見：[SCORING-SYSTEM.md](./SCORING-SYSTEM.md)

---

## 🔄 五階段循環

### 流程圖

```
┌──────────────┐
│ 1. Tutorial  │ 學習 SEO 概念
└──────┬───────┘
       ↓
┌──────────────┐
│ 2. Demo      │ 看 NPC 示範（劇情對話）
└──────┬───────┘
       ↓
┌──────────────┐
│ 3. Practice  │ 自己實作（編輯器）
└──────┬───────┘
       ↓
┌──────────────┐
│ 4. Score     │ 獲得評分與建議
└──────┬───────┘
       ↓
┌──────────────┐
│ 5. Levelup   │ 經驗值、成就、解鎖新工具
└──────┬───────┘
       ↓
   返回關卡地圖
```

### 階段切換邏輯

```javascript
// core/router.js
class PhaseRouter {
  phases = ['tutorial', 'demo', 'practice', 'score', 'levelup'];

  nextPhase() {
    const current = this.currentPhaseIndex;
    if (current < this.phases.length - 1) {
      this.loadPhase(this.phases[current + 1]);
    } else {
      this.completeLevel();
    }
  }
}
```

---

## 🚀 擴展指南

### 新增關卡

1. 在 `data/levels/world-X/` 建立新的 JSON 檔案
2. 遵循資料結構規範
3. 定義五個階段的內容
4. 設定評分標準

```bash
# 範例：新增 World 1 的第 5 關
cp data/levels/world-1/1-1.json data/levels/world-1/1-5.json
# 編輯 1-5.json 內容
```

### 新增世界

1. 建立新資料夾 `data/levels/world-4/`
2. 建立該世界的所有關卡 JSON
3. 更新關卡地圖 UI

### 新增評分維度

編輯 `data/scoring-rules.json`：

```json
{
  "criteria": {
    "newDimension": {
      "weight": 10,
      "checks": [
        "check_rule_1",
        "check_rule_2"
      ]
    }
  }
}
```

在 `modules/scoring.js` 實作檢查邏輯。

### 新增成就

編輯 `data/achievements.json`：

```json
{
  "achievements": [
    {
      "id": "new_achievement",
      "title": "成就名稱",
      "description": "達成條件說明",
      "icon": "🏆",
      "condition": "check_function_name",
      "reward": {
        "exp": 50
      }
    }
  ]
}
```

在 `modules/achievement.js` 實作條件檢查。

---

## 📝 開發規範

### 程式碼風格

- **JavaScript**：使用 ES6+ 語法
- **命名**：駝峰式（camelCase）變數，帕斯卡式（PascalCase）類別
- **註解**：重要邏輯必須註解
- **格式化**：使用 Prettier

### Git Commit 規範

```
feat: 新增功能
fix: 修復 bug
docs: 文件更新
style: 格式調整（不影響功能）
refactor: 重構程式碼
test: 測試相關
chore: 其他雜項
```

### 檔案命名

- **CSS**：小寫 + 連字號 `file-name.css`
- **JavaScript**：小寫 + 連字號 `file-name.js`
- **資料**：小寫 + 連字號 `file-name.json`

---

## 🔍 除錯指南

### 常見問題

**Q1: CSS 樣式沒有載入？**
- 檢查 `style.css` 的 `@import` 路徑
- 確認檔案名稱拼寫正確

**Q2: localStorage 資料遺失？**
- 檢查瀏覽器隱私模式
- 確認 storage.js 的 key 名稱一致

**Q3: 評分結果不正確？**
- 檢查 scoring-rules.json 權重總和是否為 100
- 確認 scoring.js 的計算邏輯

### 除錯工具

```javascript
// 開啟除錯模式
localStorage.setItem('seo_quest_debug', 'true');

// 查看當前狀態
console.log(State.current);

// 重置所有進度
Storage.reset();
```

---

## 📚 相關文件

- [README.md](./README.md) - 專案介紹
- [DATA-STRUCTURE.md](./DATA-STRUCTURE.md) - 資料結構詳細說明
- [SCORING-SYSTEM.md](./SCORING-SYSTEM.md) - 評分系統演算法
- [EXTENSION-GUIDE.md](./EXTENSION-GUIDE.md) - 擴展開發指南

---

## 📞 聯絡資訊

- **作者**：Kaoru Tsai (嚕嚕主人)
- **專案位置**：`~/helloruru.github.io/seo-quest/`
- **部署網址**：`lab.helloruru.com/seo-quest/`
- **Email**：hello@helloruru.com

---

**最後更新**：2026-02-12
**版本**：1.0.0 (基礎架構)
